const initialState = {};

function productsReducer(products = initialState, action) {
    switch (action.type) {
        default:
            return products;
    }
}

export default productsReducer;
